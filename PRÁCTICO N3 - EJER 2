#include <stdio.h>

int main() {
    int N, Z, digito;

    printf("Ingrese un numero: ");
    scanf("%d", &N);

    Z = 0;

    while (N > 0) {
        digito = N % 10;
        Z = Z * 10 + digito;
        N = N / 10;
    }

    printf("\nNumero invertido: %d\n", Z);

    return 0;
}
