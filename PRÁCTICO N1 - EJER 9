#include <stdio.h>
int main() {
    int total, h, m, s;
    printf("Ingrese cantidad de segundos: ");
    scanf("%d", &total);
    h = total / 3600;
    m = (total % 3600) / 60;
    s = total % 60;
    printf("%d Horas, %d Minutos, %d Segundos\n", h, m, s);
    return 0;
}
